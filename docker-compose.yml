services:
  catalogwatch:
    build: .
    image: catalogwatch:1.0.0
    # Optional: without a .env file the built-in defaults apply.
    env_file:
      - path: .env
        required: false
    volumes:
      - ./out:/app/out
      - ./state:/app/state
    working_dir: /app
